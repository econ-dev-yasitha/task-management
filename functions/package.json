{
  "name": "taskflow-functions",
  "description": "Cloud Functions for Taskflow task manager",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && firebase emulate:functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/generative-ai": "^0.4.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.0.6"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
